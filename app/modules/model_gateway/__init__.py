"""Model Gateway module: model registry, usage records, prompt templates, config.

The gateway *runtime* lives in ``app.platform.gateway``; this module owns the
DB-backed registry/usage/admin surface for it.
"""
