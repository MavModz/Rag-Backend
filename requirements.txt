fastapi
uvicorn[standard]

langchain
langchain-community
langchain-ollama

qdrant-client
# Hybrid BM25 sparse + cross-encoder reranking (ONNX, no torch).
fastembed
motor
# Async SQL driver for the generic SQL conversation connector (mysql+aiomysql).
# Postgres sources use asyncpg (already required below).
aiomysql

pypdf
python-docx

python-multipart
python-dotenv

httpx

# --- Platform foundation (M1) ---
# Primary relational DB (async) + migrations
sqlalchemy[asyncio]
asyncpg
alembic

# Cache / rate-limit / session store
redis

# Auth: JWT + password + API-key hashing + email validation
python-jose[cryptography]
bcrypt
email-validator

# Rate limiting
slowapi

# Object storage: local-disk backend by default (no dependency). For the S3/MinIO
# backend (OBJECT_STORE_BACKEND=s3) also install boto3:  pip install boto3

# Observability (/metrics endpoint; no Grafana/Prometheus stack required)
prometheus-client
orjson

# Model Gateway retry/backoff + OpenAI-compatible provider
# (OpenAI, NVIDIA NIM, Groq, Together, ... — any OpenAI Chat Completions endpoint)
tenacity
openai

# Worker seam (optional broker; in-process bus when unset)
celery
